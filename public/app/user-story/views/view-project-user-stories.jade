.container
    .well
        fieldset
            legend.colour4
                h1
                    span User Story
                h2
                    span.glyphicon.glyphicon-tree-deciduous.padr
                    span
                        a(ng-href="/p/{{project.code}}") {{project.name}}
            .padb
                .col-md-2.colour3.padn(ng-repeat="feature in features")
                    a.btn.btn-block.colour4(ng-href="/p/{{project.code}}/f/{{feature._id}}/u")
                        .box {{feature._id}}
                        .box
                            span.glyphicon.glyphicon-tree-deciduous(aria-label="Tree icon")
                        .box {{feature.total}}