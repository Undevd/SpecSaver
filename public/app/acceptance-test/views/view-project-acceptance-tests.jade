.container(ng-show="project")
    include ../../main/views/partials/header
    .alert.alert-info.text-center(ng-show="!stats.acceptanceTest.feature") No acceptance tests found - select a feature to create one
    div
        .col-md-2.padn(ng-class="'column' + ($index % 12 > 5 ? (6 - ($index % 6)) : (($index % 6) + 1))" ng-repeat="feature in stats.acceptanceTest.feature")
            a.btn.btn-block(ng-href="/p/{{project.code}}/f/{{feature._id}}/a")
                .box {{feature._id}}
                .box
                    span.glyphicon.glyphicon-grain(aria-label="Branch icon")
                .box {{feature.total}}